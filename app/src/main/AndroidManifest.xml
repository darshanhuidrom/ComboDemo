<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kangladevelopers.android.combodemo">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".utility.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".service.MyServiceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestRecycler" />
        <activity android:name=".Profile" />
        <activity android:name=".EditProfile" />
        <activity android:name=".AutoCompleteTest" />
        <activity android:name=".FacebookLoginActivity" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!-- <activity android:name=".FacebookTestActivity"></activity> -->

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".chat.SignInActivity" />
        <activity android:name=".PopUpWindowActivity" />
        <activity android:name=".downloader.DownloadActivity" />
        <activity android:name=".fragment.MyFragmentActivity" />
        <activity android:name=".TestRunTimePermissionActivity" />

        <!-- services -->

        <service android:name=".firebase.service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".firebase.service.MyFirebaseInstanceIDService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".notification.MyReciever"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.android.custom_broadcast"></action>
            </intent-filter>
        </receiver>

        <!-- <activity android:name=".notification.NotificationSenderActivity" /> -->
        <activity android:name=".notification.DisplayMessageActivity" />

        <service android:name=".service.MyBoundService" />

        <!--<activity android:name=".service.MyServiceActivity"></activity>-->
    </application>

</manifest>